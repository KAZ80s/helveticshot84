export class Explosion{constructor(x,y,color="#fff",size=1){this.particles=[];this.dead=false;for(let i=0;i<Math.round(16*size);i++){const a=Math.random()*Math.PI*2,s=(55+Math.random()*170)*size,l=.35+Math.random()*.55;this.particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:l,maxLife:l,color,size:Math.random()<.3?4:3})}}update(dt){let alive=false;for(const p of this.particles){if(p.life<=0)continue;alive=true;p.life-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=45*dt;p.vx*=.985;p.vy*=.985}this.dead=!alive}draw(ctx){ctx.save();for(const p of this.particles){if(p.life<=0)continue;ctx.globalAlpha=Math.max(0,p.life/p.maxLife);ctx.fillStyle=p.color;ctx.shadowColor=p.color;ctx.shadowBlur=6;ctx.fillRect(Math.round(p.x),Math.round(p.y),p.size,p.size)}ctx.restore()}}
