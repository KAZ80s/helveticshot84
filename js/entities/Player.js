import{Bullet}from"./Bullet.js";export class Player{constructor(store){this.store=store;this.width=44;this.height=24;this.x=278;this.y=720;this.speed=340;this.cooldown=0;this.lives=3;this.invulnerable=0;this.rapidTimer=0;this.shieldTimer=0;this.spreadTimer=0}reset(){this.x=278;this.y=720;this.cooldown=0;this.lives=3;this.invulnerable=0;this.rapidTimer=0;this.shieldTimer=0;this.spreadTimer=0}resetPosition(){this.x=278;this.y=720}update(dt,input){this.x+=input.axis()*this.speed*dt;this.x=Math.max(10,Math.min(546,this.x));this.cooldown=Math.max(0,this.cooldown-dt);this.invulnerable=Math.max(0,this.invulnerable-dt);this.rapidTimer=Math.max(0,this.rapidTimer-dt);this.shieldTimer=Math.max(0,this.shieldTimer-dt);this.spreadTimer=Math.max(0,this.spreadTimer-dt)}applyPowerUp(t){if(t==="rapid")this.rapidTimer=8;if(t==="shield")this.shieldTimer=10;if(t==="spread")this.spreadTimer=8}canShoot(){return this.cooldown<=0}shoot(){this.cooldown=this.rapidTimer>0?.13:.26;const cx=this.x+22,s=[new Bullet(cx,this.y-10,-580,"player")];if(this.spreadTimer>0){s.push(new Bullet(cx-6,this.y-8,-545,"player",-115),new Bullet(cx+6,this.y-8,-545,"player",115))}return s}hit(){if(this.invulnerable>0)return false;if(this.shieldTimer>0){this.shieldTimer=0;this.invulnerable=.55;return"shield"}this.lives--;this.invulnerable=1.35;this.resetPosition();return true}draw(ctx){if(this.invulnerable>0&&Math.floor(this.invulnerable*14)%2===0)return;const x=this.x,y=this.y;if(this.shieldTimer>0){ctx.save();ctx.strokeStyle="#00f5ff";ctx.shadowColor="#00f5ff";ctx.shadowBlur=10;ctx.lineWidth=2;ctx.beginPath();ctx.arc(x+22,y+12,31,0,Math.PI*2);ctx.stroke();ctx.restore()}const grid=this.store.design.ship,cellW=4,cellH=4;ctx.save();for(let r=0;r<6;r++)for(let c=0;c<11;c++){const color=grid[r][c];if(!color)continue;ctx.fillStyle=color;ctx.shadowColor=color;ctx.shadowBlur=5;ctx.fillRect(Math.round(x+c*cellW),Math.round(y+r*cellH),cellW,cellH)}ctx.restore()}get bounds(){return{x:this.x,y:this.y,width:this.width,height:this.height}}}
